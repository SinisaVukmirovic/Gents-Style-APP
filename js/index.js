const items = {
    vests: document.querySelectorAll('#vests .slider .item'),
    cardigans: document.querySelectorAll('#cardigans .slider .item'),
    watches: document.querySelectorAll('#watches .slider .item'),
}

console.log(items.vests);